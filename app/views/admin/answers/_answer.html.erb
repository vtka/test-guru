<tr>
  <td><%= answer.body %></td>
  <td><%= link_to t('actions.view'), admin_answer_path(answer) %></td>
  <td><%= link_to t('actions.edit'), edit_admin_answer_path(answer) %></td>
  <td><%= link_to t('actions.delete'), admin_answer_path(answer),
                        method: :delete,
                        data: { confirm: t('warning.confirm')} %></td>
</tr>
