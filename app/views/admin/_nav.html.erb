<nav class="navbar admin navbar-light bg-primary text-white">
  <span class="nav-admin admin title"><%= t('.header') %></span>
  <div class="nav-admin user">
    <%= t('.welcome') %> <b class="nav-admin user-email"><%= current_user.email %></b> |
    <%= link_to t('.test_path'), tests_path, class: 'text-white' %> |
    <%= link_to t('global_buttons.logout'), destroy_user_session_path, class: "text-white", method: :delete %>
  </div>
</nav>
