<div class="container">
  <h5 class="mt-4"><%= @badge.name %></h5>

  <%#= link_to t('actions.add'), new_admin_test_question_path(@test), class: 'btn btn-primary' %>
  <%= link_to t('actions.back'), admin_badges_path, class: 'btn btn-primary' %><br>

  <table class="table table-hover col-md-6 mt-3">
    <tbody>
      <%#= render @badge.questions %>
    </tbody>
  </table>

  <%= link_to t('actions.edit'), edit_admin_badge_path(@badge), class: 'btn btn-warning' %>
</div>
