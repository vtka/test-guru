<% flash.each do |key, message| %>
  <% if key == 'alert' %>
    <% key = 'danger' %>
  <% elsif key == 'notice' %>
    <% key = 'primary' %>
  <% end %>

  <%= content_tag :p, message, class: "alert alert-#{key} mt-3" %>
<% end %>
